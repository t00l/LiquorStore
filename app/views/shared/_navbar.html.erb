

<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid" style="background-color:#3f3f3f;">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="http://localhost:3000" style="margin-left:auto; margin-right:auto; vertical-align: middle;"><img alt="Gatito" src="http://findicons.com/files/icons/2579/iphone_icons/40/cat.png"></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if can? :create, Store %>
          <li><a href="/stores/new" style="color:white;">Agrega tu local<span class="sr-only">(current)</span></a></li>
        <%end%>
        <li><a href="#grupo" style="color:white;">Quiénes somos</a></li>
        <li><a href="#contacto" style="color:white;">Contáctanos</a></li>

        <form class="navbar-form navbar-left" role="search" action="/">
          <div class="form-group">
            <input type="text" class="form-control" placeholder="Busca por botillería o promo..." style="width:220px;" name="query" id="query">
          </div>
          <button type="submit" class="btn btn-default">Buscar</button>
        </form>

        <ul class="nav navbar-nav navbar-right">            
          <% if owner_signed_in? %>
            <li><%= link_to current_owner.name, edit_owner_registration_path %></li>
            <li><%= link_to 'Salir', destroy_owner_session_path, method: :delete, data: { confirm: 'Seguro que deseas salir?'} %></li>
          <% elsif user_signed_in?%>
            <li><%= link_to current_user.name, edit_user_registration_path %></li>
            <li><%= link_to 'Salir', destroy_user_session_path, method: :delete, data: { confirm: 'Seguro que deseas salir?'} %></li>
          <%else%>

<!--         SIN DROPDOWN!
            <li><%= link_to 'Ingresar owner', new_owner_session_path %></li>
            <li><%= link_to 'Ingresar', new_user_session_path %></li>
            <li><%= link_to 'Registrar', new_user_registration_path %></li>
            <li><%= link_to 'Registrar owner', new_owner_registration_path %></li>
 -->
           <!--  CON DROPDOWN! -->

          <li class="dropdown">
             <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="color:white;">Usuario?<span class="caret"></span></a>
              <ul id=”dropdown-login” class="dropdown-menu" >
                  <%= form_for(:user, :url => user_session_path) do |f| %>
                  <div class="field">
                    <%= f.label :email %><br />
                    <%= f.email_field :email, autofocus: true %>
                  </div>

                  <div class="field">
                    <%= f.label :password %><br />
                    <%= f.password_field :password, autocomplete: "off" %>
                  </div>

                  <% if devise_mapping.rememberable? -%>
                    <div class="field">
                      <%= f.check_box :remember_me %>
                      <%= f.label :remember_me %>
                    </div>
                  <% end -%>

                  <div class="actions">
                    <%= f.submit "Log in", class: "btn btn-primary" %>
                  </div>
                <% end %>
                <%= link_to "Login with Facebook", user_omniauth_authorize_path(:facebook)%>
                <span class="label label-success"><%=link_to 'Registro como usuario', new_user_registration_path %></span>
              </ul>
          </li>

            <li class="dropdown">
             <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="color:white;">Registro<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Ingresar', new_user_session_path %></li>
                <li><%= link_to 'Registrar', new_user_registration_path %></li>
              </ul>
            </li>

            <li class="dropdown">
               <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="color:white;">Dueño?<span class="caret"></span></a>
                <ul class="dropdown-menu">
                <li><%= link_to 'Ingresar owner', new_owner_session_path %></li>
                <li><%= link_to 'Registrar owner', new_owner_registration_path %></li>
                </ul>
            </li>
            <!-- FIN DROPDOWN -->
          <% end %>
        </ul>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>


  <script>

      $(document).ready(function(){
          $('.navbar .dropdown').hover(function() {
              $(this).find('.dropdown-menu').first().stop(true, true).delay(250).slideDown();
          }, function() {
              $(this).find('.dropdown-menu').first().stop(true, true).delay(100).slideUp()
          });
      })
  </script>

  <script>
    // Prevent closing the sign in form on click
    j$(‘#dropdown-login’).click(function(e) {
       e.stopPropagation();
    );
  </script>